[package]
edition = "2024"
name = "whostoblame"
readme = "README.md"
version = "0.1.0"

[dependencies]
rayon = { version = "1.11.0", optional = true }
regex-lite = { version = "0.1.9" }

[features]
default = [
  "parallelization",
]
parallelization = [
  "dep:rayon",
]
